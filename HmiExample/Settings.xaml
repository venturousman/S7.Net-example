<Page x:Class="HmiExample.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HmiExample"
      mc:Ignorable="d" Loaded="SettingsPage_Loaded"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Settings">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox Name="gbSettings" Header="App Settings"
                  HorizontalAlignment="Left" VerticalAlignment="Top"
                  Width="400" Height="300" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mold life" Margin="10" Width="100" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"></TextBlock>
                <TextBox Grid.Row="0" Grid.Column="1" Name="txtMoldLife" 
                         Margin="10" PreviewTextInput="OnPreviewTextInput" Height="40"></TextBox>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Max cycle time" Margin="10" Width="100" 
                           HorizontalAlignment="Left" VerticalAlignment="Center"></TextBlock>
                <TextBox Grid.Row="1" Grid.Column="1" Name="txtMaxCycleTime" 
                         Margin="10" PreviewTextInput="OnPreviewTextInput" Height="40"></TextBox>

            </Grid>
        </GroupBox>
        <StackPanel Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Button Content="Save changes" Name="btnSaveSettings" Click="btnSaveSettings_Click" 
                    Padding="6,4,6,4"></Button>
        </StackPanel>
    </Grid>
</Page>
